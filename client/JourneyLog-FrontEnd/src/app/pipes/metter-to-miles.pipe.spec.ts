import { MetterToMilesPipe } from './metter-to-miles.pipe';

describe('MetterToMilesPipe', () => {
  it('create an instance', () => {
    const pipe = new MetterToMilesPipe();
    expect(pipe).toBeTruthy();
  });
});
